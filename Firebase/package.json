{
  "name": "firebase",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "firebase": "firebase",
    "emulators": "NODE_ENV=development firebase emulators:start",
    "deploy": "NODE_ENV=production firebase deploy",
    "deploy:rules": "NODE_ENV=production firebase deploy --only firestore:rules,storage:rules",
    "deploy:functions": "NODE_ENV=production firebase deploy --only functions",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "dotenv": "^16.4.1"
  },
  "devDependencies": {
    "firebase-tools": "13.0.0"
  }
}